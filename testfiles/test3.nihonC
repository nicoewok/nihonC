#include <stdio.h>

int main() 「
    int i;

    enum Level {
        LOW,
        MEDIUM,
        HIGH
    };

    struct Person {
        char name[100];
        int age;
    };

    union Data {
        int i;
        float f;
        char str[20];
    };
    //If statement
    int number = 10。
    if (number > 5) 「
        printf("Number is greater than 5\n")。
    」 else 「
        printf("Number is 5 or less\n")。
    」

    // While loop
    整数 count は 0 です。
    while (count < 5) 「
        printf("Count is %d\n", count)。
        count++。
    」

    // For loop
    整数 0 から 5 まで i 「
        printf("i is %d\n", i)。
    」
    

    return 0。
」